import { AddressSchema } from '@/schemas/common';

/**
 * @deprecated Use AddressSchema from '@/schemas/common' instead.
 * This alias is maintained for legacy imports but introduces the new Address structure.
 */
export const LocationSchema = AddressSchema;
